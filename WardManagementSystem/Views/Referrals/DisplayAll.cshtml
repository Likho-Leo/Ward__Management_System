@model IEnumerable<WardDapperMVC.Models.Domain.PatientReferrals>

@{
    ViewData["Title"] = "Referrals List";
}
<div class="wide-dark-box">
    <h2 class="text-center">MANAGE PATIENT REFERRALS</h2>
    <br />
    <p>
        Welcome to the Manage Patient referrals section of our Ward Management System,
        where you can seamlessly refer a new patient to be admitted.
    </p>
    <div class="table-responsive">
        <table class="table table-bordered">
            <thead class="table-dark">
                <tr>
                    <th>Patient FirstName</th>
                    <th>Patient LastName</th>
                    <th>Referral Date</th>
                    <th>Doctor FullName</th> <!-- Header changed -->
                    <th>Status</th>
                    @* <th>Actions</th> *@
                </tr>
            </thead>
            <tbody class="table-light">
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.PatientFirstName</td>
                        <td>@item.PatientLastName</td>
                        <td>@item.Date.ToString("d")</td>
                        <td>@item.FirstName @item.LastName</td> <!-- Displaying Doctor's Name -->
                        <td>@item.Status</td>
                        @*  <td>
                    <a asp-action="Details" asp-route-id="@item.ReferPatientID" class="btn btn-info">Details</a>
                    <a asp-action="Edit" asp-route-id="@item.ReferPatientID" class="btn btn-warning">Edit</a>
                    <a asp-action="Delete" asp-route-id="@item.ReferPatientID" class="btn btn-danger">Delete</a>
                    </td> *@
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <a asp-action="Create" class="btn btn-success">Create New Referral</a>
    <br />
    <div id="notification" style="display:none; padding: 10px;">
        @if (TempData["msg"] != null)
        {
            <div class="mb-3 alert alert-info">
                @TempData["msg"]
            </div>
        }
    </div>
</div>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function () {
            // Show notification if it exists
            if ($('#notification').text().trim() !== '') {
                $('#notification').fadeIn().delay(3000).fadeOut(); // Display for 3 seconds
            }
        });
    </script>
}

@* <style>
    .table {
        color: white; /* Change text color for table cells */
    }

        .table thead th {
            background-color: #003366; /* Darker background for the header */
            color: white; /* White text for header */
        }

        .table tbody td {
            background-color: rgba(0, 0, 0, 0.5); /* Optional: Dark background for table rows */
            color: white; /* White text for table cells */
        }

        .table tbody tr:hover {
            background-color: rgba(0, 0, 0, 0.7); /* Darker background on row hover */
        }
</style> *@
