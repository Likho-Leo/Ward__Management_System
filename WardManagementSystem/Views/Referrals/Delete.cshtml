@* @model WardDapperMVC.Models.Domain.PatientReferrals

@{
    ViewData["Title"] = "Delete Referral";
}

<h1>Delete Referral</h1>

<div>
    <h4>Are you sure you want to delete this referral?</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Refer Patient ID</dt>
        <dd class="col-sm-10">@Model.ReferPatientID</dd>

        <dt class="col-sm-2">First Name</dt>
        <dd class="col-sm-10">@Model.FirstName</dd>

        <dt class="col-sm-2">Last Name</dt>
        <dd class="col-sm-10">@Model.LastName</dd>

        <dt class="col-sm-2">Date</dt>
        <dd class="col-sm-10">@Model.Date.ToString("d")</dd>

        <dt class="col-sm-2">Doctor ID</dt>
        <dd class="col-sm-10">@Model.</dd>

        <dt class="col-sm-2">Doctor First Name</dt>
        <dd class="col-sm-10">@Model.FirstName</dd>

        <dt class="col-sm-2">Doctor Last Name</dt>
        <dd class="col-sm-10">@Model.LastName</dd>

        <dt class="col-sm-2">Status</dt>
        <dd class="col-sm-10">@Model.Status</dd>
    </dl>
</div>

<div>
    <form asp-action="Delete" asp-route-id="@Model.ReferPatientID" method="post">
        <input type="hidden" name="id" value="@Model.ReferPatientID" />
        <button type="submit" class="btn btn-danger">Delete</button>
        <a asp-action="DisplayAll" class="btn btn-primary">Cancel</a>
    </form>
</div>

<div>
    @if (TempData["msg"] != null)
    {
        <div class="alert alert-info">@TempData["msg"]</div>
    }
</div>
 *@