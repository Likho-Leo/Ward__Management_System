@model IEnumerable<WardDapperMVC.Models.Domain.PatientMovement>
@{
    ViewData["Title"] = "DisplayAllMovements";
}

<div class="wide-dark-box">
    <h1 class="text-center">MANAGE PATIENT MOVEMENTS</h1>
    <br />
    <p>
        Welcome to the Manage Patient Movement section of our Ward Management System,
        where you can search for patients using their number, track their location changes, and update their movement records.
    </p>

    <div class="table-responsive">
        <!-- Search Form -->
        <form method="get" asp-controller="PatientMovement" asp-action="DisplayAllMovements" class="mb-3">
            <div class="input-group">
                <input type="text" name="search" class="form-control" placeholder="Search by Patient Number"
                       aria-label="Search by Patient Number" value="@Context.Request.Query["search"]" style="max-width: 300px;" />
                <button class="btn btn-outline-secondary" type="submit">Search</button>
            </div>
        </form>

        <table class="table table-bordered">
            <thead class="table-dark">
                <tr>
                    <th>Patient Number</th>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Ward Name</th>
                    <th>Bed Number</th>
                    <th>Date</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody class="table-light">
                @foreach (var movement in Model)
                {
                    <tr>
                        <td>@movement.PatientNumber</td>
                        <td>@movement.FirstName</td>
                        <td>@movement.LastName</td>
                        <td>@movement.WardName</td>
                        <td>@movement.BedNo</td>
                        <td>@movement.Date.ToString("yyyy-MM-dd")</td> <!-- Formatting date -->
                        <td>@movement.Status</td>
                        <td white-space: nowrap>
                            <a asp-controller="PatientMovement" asp-action="EditMovement" asp-route-id="@movement.MovementID" class="btn btn-info my-2">Edit</a>
                            <a asp-controller="PatientMovement" asp-action="DeleteMovement" asp-route-id="@movement.MovementID" class="btn btn-danger" onclick="return window.confirm('Are you sure you want to delete this record? This action cannot be undone.')">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <a asp-controller="PatientMovement" asp-action="AddMovement" class="btn btn-success">Add more</a>
    <br />
    <div id="notification" style="display:none; padding: 10px;">
        @if (TempData["msg"] != null)
        {
            <div class="mb-3 alert alert-info">
                @TempData["msg"]
            </div>
        }
    </div>
</div>

<style>
    /* Search bar styling */
    .input-group {
        max-width: 300px; /* Set a max width for the search bar */
        margin-bottom: 20px; /* Add margin below the search bar */
    }

    /* Highlight row on hover */
    .table tbody tr:hover {
        background-color: #f1f1f1; /* Light gray background */
    }

    /* Optional: Make the table more visually appealing */
    .table {
        margin-top: 20px;
    }
</style>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function () {
            // Show notification if it exists
            if ($('#notification').text().trim() !== '') {
                $('#notification').fadeIn().delay(3000).fadeOut(); // Display for 3 seconds
            }
        });
    </script>
}




